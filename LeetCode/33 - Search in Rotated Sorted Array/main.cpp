#include <iostream>
#include <vector>

int search(std::vector<int>& nums, int target) {
    return 0;
}

int main() {
    std::vector<int> input = { 4, 5, 6, 7, 0, 1, 2 };
    std::cout << search(input, 0) << std::endl;
    return 0;
}